FILE(GLOB REOS_UIS "${CMAKE_CURRENT_SOURCE_DIR}/*.ui")
FILE(GLOB 3D_UIS "${CMAKE_CURRENT_SOURCE_DIR}/3D/*.ui")
FILE(GLOB SIMULATION_ENGINES_UIS "${CMAKE_CURRENT_SOURCE_DIR}/simulationEngine/*.ui")

QT5_WRAP_UI(REOS_UIS_H
  ${REOS_UIS}
  ${3D_UIS}
  ${SIMULATION_ENGINES_UIS}
)

ADD_CUSTOM_TARGET(ui ALL DEPENDS ${REOS_UIS_H})
